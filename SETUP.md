# 3D Print Manager Setup Guide (A1 Combo / AMS Lite Edition)

ระบบคำนวณต้นทุนงานพิมพ์ 3D ที่ออกแบบมาเพื่อรองรับการพิมพ์หลายสี (Multi-color) โดยเฉพาะสำหรับผู้ใช้งาน Bambu Lab A1 Combo พร้อม AMS Lite

## 1. การเตรียมตัวใน Google Sheets
1.  สร้างไฟล์ Google Sheets ใหม่
2.  ไปที่เมนู **Extensions > Apps Script**
3.  นำโค้ดจากไฟล์ `Code.gs` ไปวางทับของเดิม
4.  สร้างไฟล์ HTML ใหม่ใน Apps Script ตั้งชื่อว่า `index` และนำโค้ดจาก `index.html` ไปวาง
5.  กด **Save** และ **Deploy** เป็น Web App (เลือก "Anyone" ในส่วน Who has access)

## 2. การสร้างโครงสร้างตาราง
1.  ในหน้า Google Sheets จะมีเมนูใหม่ชื่อ **3D Print Manager**
2.  เลือก **1. สร้างโครงสร้างตาราง (Setup)** ระบบจะสร้างหน้า Settings, Inventory, Calculator, และ Orders ให้โดยอัตโนมัติ
3.  **สำคัญ:** ระบบจะตั้งค่าเริ่มต้นสำหรับ A1 Combo ไว้ที่ **120 วัตต์** และ **ค่าเสื่อม 3 บาท/ชม.**

## 3. การกรอกข้อมูลเบื้องต้น
*   **Settings Sheet:** ตรวจสอบค่าไฟ (B1) และค่าแรง (B2) ให้ตรงตามที่คุณต้องการ
*   **Inventory Sheet:** ใส่ชื่อเส้นพลาสติกและราคาม้วนละเท่าไหร่ (อย่าลืมใส่ Total Weight และ Cost per Spool เพื่อให้ระบบคำนวณราคาต่อกรัมให้)
*   **Sync Data:** ทุกครั้งที่เพิ่มเส้นพลาสติกใหม่ ให้กดเมนู **3D Print Manager > 2. อัปเดตรายชื่อเครื่อง/เส้น (Sync)**

## 4. วิธีการใช้งาน (หน้า Calculator)
1.  **เลือกเครื่องพิมพ์:** (A1 Combo)
2.  **เลือกเส้นพลาสติก:** สามารถเลือกได้สูงสุด 4 เส้น (Slot 1-4) ตาม AMS Lite
3.  **กรอกน้ำหนัก:** ใส่น้ำหนักของแต่ละสีที่ใช้ (ดูได้จาก Slicer)
4.  **Wipe Tower / Waste:** กรอกน้ำหนักพลาสติกที่ทิ้ง (Poop/Tower) จาก Slicer เพื่อให้ต้นทุนแม่นยำที่สุด
5.  **คำนวณ:** กดปุ่มคำนวณในเมนู หรือใช้งานผ่านลิงก์ **Web App** บนมือถือได้เลยครับ

## 5. การดูต้นทุน
*   **Filament Cost:** รวมราคาเส้นพลาสติกทุกสี + ราคาพลาสติกที่ทิ้ง (Waste)
*   **Suggested Price:** ราคาแนะนำที่รวมค่าไฟ ค่าแรง ค่าเสื่อม และกำไรที่ระบุไว้แล้ว

---
*ระบบนี้ถูกออกแบบมาเพื่อให้คุณไม่ขาดทุนจาก "ต้นทุนแฝง" เช่น พลาสติกที่ทิ้งตอนเปลี่ยนสีและค่าเสื่อมสภาพของเครื่องพิมพ์ความเร็วสูงครับ*
